class TLPageSettings {
    public NoTitle: boolean = true; // テキストとは別に見出しを設定するかどうか(trueのときテキストの先頭部分が見出しになる)
    //public NoEdit: boolean = false; // 編集可能かどうか(trueのとき編集不可)
    public PageLoad: boolean = false; // ページごとのロードをするかどうか
}
